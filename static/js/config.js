// AgriVision AI - Frontend Configuration
// =============================================================================
// When deploying frontend on Vercel and backend on Render:
// Replace the empty string below with your Render backend URL, e.g.:
// window.API_BASE_URL = "https://agrivision-backend.onrender.com";
// When running locally, leave it as "" (empty string).
// =============================================================================
window.API_BASE_URL = window.API_BASE_URL || "";
